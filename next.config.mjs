/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  // devIndicators: false, //para quitar el icon de next
};

export default nextConfig;
